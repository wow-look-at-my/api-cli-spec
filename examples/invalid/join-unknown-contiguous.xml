<?xml version="1.1" encoding="UTF-8"?>
<!-- rejects: attribute "contiguous" on element "join": value "strict" is not one of the allowed values: warn, error -->
<config name="x">
	<command name="c">
		<download group="{{ .stream }}" order="{{ .index }}">
			<url>https://example.invalid/f</url>
			<to>parts/f</to>
			<join to="out/all.ts" contiguous="strict"/>
		</download>
	</command>
</config>
