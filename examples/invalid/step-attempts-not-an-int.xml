<?xml version="1.1" encoding="UTF-8"?>
<!-- rejects: attribute "attempts" on element "step": "many" is not a valid integer -->
<config name="x">
	<command name="c">
		<steps>
			<step name="poll" until="{{ .result.poll.ok }}" interval="5s" attempts="many">
				<run>check</run>
			</step>
		</steps>
		<run>x</run>
	</command>
</config>
